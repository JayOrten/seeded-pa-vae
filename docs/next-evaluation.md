# evaluations to run

could also be cool to train multiple models with different parameters and see how much it can specialize

the degree distribution graph should technically be a log scale to show the scale-free property, but we are building large enough graphs to do that, so we should scale up.

we should test structural invariants without enforcing them

another useful baseline would just be an algorithm that randomly attaches to any point in the graph, no PA

could be cool to train a classifier on the two different kidns of graphs to see if it can learn to tell them apart.