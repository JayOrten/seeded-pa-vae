# evaluations to run

could also be cool to train multiple models with different parameters and see how much it can specialize

the degree distribution graph should technically be a log scale to show the scale-free property, but we are building large enough graphs to do that, so we should scale up.
